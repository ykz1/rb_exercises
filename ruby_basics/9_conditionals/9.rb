number = rand(10)

if number == 5
  puts '5 is a cool number!'
else
  puts 'Other numbers are cool too!'
end

# the error was that line 3 originally assigned integer 5 to variable number with `=`, and needs to be replaced with comparison operator `==` 
